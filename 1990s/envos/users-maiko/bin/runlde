# First comment line required (invokes CSH)# =============================================================================# Relies on shell variable MYSYSOUT (which can be set in your .login or .cshrc)# to start up lisp.  Otherwise, uses the normal default for lde(ether).## Also relies on shell variable LDE, otherwise assumes ldeether.## Uses script 'runkey' to provide host key.# =============================================================================if ($?LDE != '1') then	setenv LDE ldeether	endifif ("'`runkey`'" == "'host-unknown'") then	echo ""	echo "Sorry, host '`hostname`' not found in runkey script."	echo "Check /users/maiko/bin/runkey" 	echo ""	exit	endifecho $LDE $1 -k "`runkey`"     $LDE $1 -k "`runkey`"