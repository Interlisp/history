/* This is G-file @(#) timeout.h Version 1.8 (1/5/89). copyright Xerox & Fuji Xerox  */extern jmp_buf jmpbuf;/*** TIMEOUT_TIME is changeable by UNIX env var LDEFILETIMEOUT. #define	TIMEOUT_TIME	10 **/extern	int	TIMEOUT_TIME;#define	SETJMP(x){				\		if(setjmp(jmpbuf) != 0)	{	\			return(x);		\		}				\	}#define	TIMEOUT(exp){			\		alarm(TIMEOUT_TIME);	\		exp;			\		alarm(0);		\	}#define	S_TOUT(exp)	\		alarm(TIMEOUT_TIME),\		exp,			\		alarm(0)#define	ERRSETJMP(rval){			\		 if(setjmp(jmpbuf) != 0){		\			*Lisp_errno = 100;		\			return(rval);			\		 }					\		}