/* This is G-file @(#) timeout.h Version 1.5 (8/1/88). copyright Xerox & Fuji Xerox  */extern jmp_buf jmpbuf;#define	TIMEOUT_TIME	10#define	SETJMP(x){				\		if(setjmp(jmpbuf) != 0)		\			return(x);		\	}#define	TIMEOUT(exp){			\		alarm(TIMEOUT_TIME);	\		exp;			\		alarm(0);		\	}#define	S_TOUT(exp)	\		alarm(TIMEOUT_TIME),\		exp,			\		alarm(0)#define	ERRSETJMP(rval, errp){				\		 if(setjmp(jmpbuf) != 0){		\			*errp = 100;			\			return(rval);			\		 }					\		}