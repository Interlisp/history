#ciall: checkin all edited files on your current directory.#set files=`ls *.c`foreach file ($files)	if (-w $file) then		ci $file	endifend#set files=`ls *.il`foreach file ($files)	if (-w $file) then		ci $file	endifend#set files=`ls *.h`foreach file ($files)	if (-w $file) then		ci $file	endifend#set files=`ls makefile-*`foreach file ($files)	if (-w $file) then		ci $file	endifend#set files=`ls *.s`foreach file ($files)	if (-w $file) then		ci $file	endifend#set files=`ls *.lex`foreach file ($files)	if (-w $file) then		ci $file	endifend