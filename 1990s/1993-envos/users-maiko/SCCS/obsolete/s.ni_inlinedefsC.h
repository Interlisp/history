h35896s 00003/00000/00206d D 1.5 88/04/22 19:24:35 krivacic 5 4c ***** FILE OBSOLETED *****es 00003/00003/00203d D 1.4 88/03/31 15:10:42 krivacic 4 3c storen, copyn, arg0, assoces 00004/00000/00202d D 1.3 88/03/31 10:06:29 krivacic 3 2c N interfaces for get&put bytes etces 00001/00001/00201d D 1.2 88/03/28 11:35:21 krivacic 2 1c added op_ufn labeles 00202/00000/00000d D 1.1 88/03/23 10:10:16 krivacic 1 0c date and time created 88/03/23 10:10:16 by krivaciceuUtTI 1/* This is G-file %Z% %M% Version %I% (%G%). copyright Xerox & Fuji Xerox  *//* This is G-file %Z% %M% Version %I% (%G%). copyright Xerox & Fuji Xerox  *//* 	ni_inlinedefsC.h *//* 	Bob Krivacic 2/23/88 */I 5*************************************************************/* ******** THIS FILE IS OBSOLETE DO NOT USE ************ */*************************************************************E 5/* 		These are the Macros Used to generate inline c code.	These are the goto ni definitions of the opcodes.*/#undef ADDBASE#undef LOLOC#undef HILOC#undef VAG2#define SWAP_WORDS(x) \	((((unsigned int) x << 16) & 0xFFFF0000) | (((unsigned int) x >> 16) & 0xFFFF))#define CHECK_INTERRUPT {if(--LoopCounter<0) goto check_interrupt;} #define NATIVECHECK							\	{if (BCE_CURRENTFX->native) {goto gonative ;} nextop0; }#define nextop0 {int i; i = Get_BYTE(PCMAC); goto nextopcode; }#define nextop1 {PCMAC += 1; nextop0; }#define nextop2 {PCMAC += 2; nextop0; }#define nextop3 {PCMAC += 3; nextop0; }#define nextop4 {PCMAC += 4; nextop0; }#define UFN(x)  { EXT; ufn((x)); NRET; }#define	IVARMACRO(x)	{ goto ni; }#define	PVARMACRO(x)	{ goto ni; }#define	FVARMACRO(x)	{ goto ni; }#define	PVARSETMACRO(x)	{ goto ni; }#define	PVARSETPOPMACRO(x) { goto ni; }#define	PUSHATOM(x)	{ goto ni; }#define	JUMPMACRO(x)	{ goto ni; }#define	FJUMPMACRO(x)	{ goto ni; }#define	TJUMPMACRO(x)	{ goto ni; }#define GETBASE_N(n)	{ goto ni; }#define GETBASEPTR_N(n) { goto ni; }#define PVARX(x)	{ goto ni; }#define PVARX_(x)	{ goto ni; }#define IVARX(x)	{ goto ni; }#define IVARX_(x)	{ goto ni; }#define GVAR(x)		{ goto ni; }#define COPY		{ goto ni; }#define SWAP		{ goto ni; }#define FN0		{ CHECK_INTERRUPT; goto ni; }#define FN1		{ CHECK_INTERRUPT; goto ni; }#define FN2		{ CHECK_INTERRUPT; goto ni; }#define FN3		{ CHECK_INTERRUPT; goto ni; }#define FN4		{ CHECK_INTERRUPT; goto ni; }#define FNX		{ CHECK_INTERRUPT; goto ni; }#define APPLY		{ CHECK_INTERRUPT; goto ni; }#define CHECKAPPLY	{ CHECK_INTERRUPT; goto ni; }#define RETURN		{ CHECK_INTERRUPT; goto ni; }#define ATOMCELL_N(n)	{ goto ni; }#define BLT		{ goto ni; }I 3#define GETBASEBYTE	{ goto ni; }E 3#define PUTBASEBYTE	{ goto ni; }#define PUTBASEPTR_N(n)	{ goto ni; }#define PUTBASE_N(n)	{ goto ni; }I 3#define GETBITS_N_M(a,b) { goto ni; }#define PUTBITS_N_M(a,b) { goto ni; }E 3I 3D 4#define INSTANCEP	{ goto ni; }E 4I 4#define INSTANCEP(atom_index)	{ goto ni; }E 4E 3#define DIFFERENCE	{ goto ni; }#define PLUS		{ goto ni; }#define	GREATERP	{ goto ni; }#define IPLUS_N(n)	{ goto ni; }#define IDIFFERENCE_N(n){ goto ni; }#define QUOTIENT 	{ goto ni; } #define TIMES2		{ goto ni; } #define IREMAINDER 	{ goto ni; }#define LOGOR	{ goto ni; } #define LOGAND	{ goto ni; }#define LOGXOR	{ goto ni; }#define LRSH1	{ goto ni; }#define LRSH8	{ goto ni; }#define LLSH1	{ goto ni; }#define LLSH8	{ goto ni; }							#define ADDBASE { goto ni; }#define LOLOC	{ goto ni; }#define HILOC	{ goto ni; }#define VAG2	{ goto ni; }#define OPCAR	{ goto ni; }#define OPCDR	{ goto ni; }#define RPLACA	{ goto ni; }#define RPLACD	{ goto ni; }#define LISTP	{ goto ni; }#define NTYPEX	{ goto ni; }D 4#define TYPEP	{ goto ni; }#define DTEST	{ goto ni; }E 4I 4#define TYPEP(n)	{ goto ni; }#define DTEST(atom_index)	{ goto ni; }E 4#define BIND		{ goto ni; }#define UNBIND		{ goto ni; }#define DUNBIND		{ goto ni; }#define RPLPTR		{ goto ni; }#define GCREF		{ goto ni; }#define ASSOC		{ goto ni; }#define CONS		{ goto ni; }#define DBX		{ goto ni; }#define FMEMB		{ goto ni; }#define FINDKEY		{ goto ni; }#define CREATECELL	{ goto ni; }#define BIN		{ goto ni; }#define RPLCONS		{ goto ni; }#define LISTGET		{ goto ni; }#define EVAL		{ goto ni; }#define STKSCAN		{ goto ni; }#define RECLAIMCELL	{ goto ni; }#define GCSCAN1		{ goto ni; }#define GCSCAN2		{ goto ni; }#define CONTEXTSWITCH	{ goto ni; }#define UBFLOAT3(n)	{ goto ni; }#define TYPEMASK	{ goto ni; }#define EQLOP		{ goto ni; }#define DRAWLINE	{ goto ni; }#define STOREN		{ goto ni; }#define COPYN		{ goto ni; }#define GVAR_(x)	{ goto ni; }#define FVARX(x)	{ goto ni; }#define FVARX_(x)	{ goto ni; }#define MAKENUMBER 	{ goto ni; }#define BOXIPLUS	{ goto ni; }	#define BOXIDIFFERENCE	{ goto ni; }						#define AREF1		{ goto ni; }#define AREF2		{ goto ni; }#define ASET1		{ goto ni; }#define ASET2		{ goto ni; }#define MISC3(n)	{ goto ni; }#define MISC4(n)	{ goto ni; }#define CLEQUAL		{ goto ni; }#define ILEQUAL		{ goto ni; }#define CLARITHEQUAL	{ goto ni; }#define UBFLOAT1(n)	{ goto ni; }#define UBFLOAT2(n)	{ goto ni; }#define RESTLIST	{ goto ni; }#define CLFMEMB		{ goto ni; }#define CLASSOC		{ goto ni; }#define FPLUS2		{ goto ni; }#define FDIFFERENCE	{ goto ni; }#define FTIMES2		{ goto ni; }#define FQUOTIENT	{ goto ni; }#define FGREATERP	{ goto ni; }#define DEFAULT								\ni: 	{	asm("ni:");						\		EXT;							\		(*(OPtbl_base + Get_BYTE(PCMAC)))();			\ 		RET; nextop0; 						\	}#define OPCODEFAIL							\									\OP_fix_2:								\	asm("fixtos2:");						\	FIXTOS1;							\OP_fix_1:								\	asm("fixtos1:");						\	FIXTOS1;							\OP_fix_0:								\	EXT;								\  	(*(OPtbl_base + Get_BYTE(PCMAC)))();				\   	RET;								\  	nextop0;#define	UFN_CALLS							\									\D 2ufn_0:	EXT;								\E 2I 2op_ufn:	EXT;								\E 2	ufn(Get_BYTE(PCMAC));						\	RET;								\	nextop0;E 1