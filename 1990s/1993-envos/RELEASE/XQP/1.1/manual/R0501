ref-5-1                    Xerox Quintus Prolog Reference Manual                             Section 5-1: +P , +QConjunction:  (P , Q) succeeds if P succeeds and then Q succeeds.  This is notnormally regarded as a built-in predicate, since it is part of the syntax ofthe language.  However, it is like a built-in predicate in that you can saycall((P , Q)) to execute P and then Q.