ref-5-2-6                    Xerox Quintus Prolog Reference Manual                            Section 5-2-6: tab(+N)tab(N) writes N spaces to the current output stream.  N may be an integerexpression.If N is not a valid arithmetic expression, then tab(N) fails and sends an errormessage to the standard error stream.  If N evaluates to a negative integer,tab(N) simply succeeds without doing anything.  If N evaluates to a float,tab(N) simply fails.If the current output device is the user's terminal, the spaces are notnecessarily printed immediately; see ttyflush/0, {manual(ref-5-7-7)}.