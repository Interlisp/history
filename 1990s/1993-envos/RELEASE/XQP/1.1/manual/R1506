ref-15-6                    Xerox Quintus Prolog Reference Manual                          Section 15-6: erase(+Ref)erase(Ref) erases from the data base the dynamic clause (or recorded term --see {manual(ref-16)}) referenced by Ref.  Any subsequent attempt to use thespecified reference will fail.If Ref is not a data base reference to an existing clause or recorded term,erase/1 simply fails.